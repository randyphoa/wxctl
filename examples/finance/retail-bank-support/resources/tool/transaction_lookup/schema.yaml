input_schema:
  properties:
    account_id:
      description: The account identifier whose transactions to list (e.g., "ACC-1001")
      type: string
  required:
  - account_id
  type: object
output_schema:
  type: object
